{% extends 'base.twig' %}
{% block body %}
    <h1>{{ project.name }}</h1>
    <p>{{ project.description | raw('html')}}</p>
    <p>du{{ project.startedAt.format('d-m-Y')  }} au {{ project.finishedAt.format('d-m-Y à H:i:s')  }} </p>
    <p>voici les langages</p>



{% endblock %}




















